# OwnBox v2.8.14-preview

* 彻底修复策略组设置中除“最低延迟”外的 4 种策略（最低负载、随机选择、轮询、一致性哈希）不起作用并退化为最低延迟的问题：
  - 严格保障 5 种策略各行其道：最低延迟 (leastPing)、最低负载 (leastLoad)、随机选择 (random)、轮询 (roundRobin)、一致性哈希 (consistentHash)
  - 完整保留“最低延迟”原有的高效测速探测、容差防抖与快速故障转移机制，体验与原版完全一致
  - 修复“轮询 (roundRobin)”在具有目的域名/IP 时错误进行目的哈希导致固定节点粘滞的问题，恢复为纯正的按连接请求顺序轮换分发
  - 修复“最低负载 (leastLoad)”在连接数较小时被目的哈希强行覆盖的缺陷，严格依据节点实时活跃连接数（activeConns）调度，同负载节点平滑均匀分配
  - 移除针对非最低延迟策略的 800ms~2000ms 过激动态超时，避免正常海外节点因跨境握手耗时被误判超时并降级，确保所有健康节点享有平等的调度与建连机会
  - 隔离周期性 URLTest 探测任务，仅在“最低延迟”策略下激活背景测速，防止非延迟策略被测速延迟与状态污染
* 递增版本号至 2.8.14 预览版（versionCode 317），支持无损覆盖升级安装
