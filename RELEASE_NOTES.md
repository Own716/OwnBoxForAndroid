# OwnBox v2.7.8 正式版 (v2.7.8)

* 【内核缓存自愈】精准拦截底层 bbolt / cachefile 数据库物理损坏异常（如 `invalid freelist page` / `initialize cache-file: timeout` 等致命 Panic），引入物理熔断与静默自愈机制：严格限制单次最多重试 1 次，自动主动静默物理清除各候选路径下损坏残留的 `cache.db` 并触发无缝重建重启内核，无需用户手动清除应用数据。
* 【优雅退出与刷盘保护】优化内核停止与切换节点时的资源释放流程，将 `proxy.close()` 调度至后台 IO 线程充分完成脏页同步刷盘（Sync）与资源卸载，并在重启前提供文件锁与文件描述符释放缓冲，彻底消除连续切换节点引发的文件锁竞争与脏页 Panic。
* 【走马灯留白优化】在负载均衡节点卡片中，为服务器详细参数与所属分组名称之间注入 6 字符视觉留白缓冲，消除两端紧贴拥挤，提供平滑驻留动效与舒适阅读体验。
* 【卡片占位符修复】修复负载均衡设置卡片右侧未设值时泄露 `%1$s↑ %2$s↓` 模板占位符的 UI 缺陷，并在布局层面使用预览占位，确保在各类状态下卡片呈现纯净优雅。
* 【全局文档与指南补齐】在侧边栏使用指南中全量补齐负载均衡切换容差 (Tolerance) 与双单位 (ms / s) 的核心工作机制解析与黄金推荐值；全面强化 1:1 官方纯正 sing-box 仪表盘（涵盖多 Tab 全景监控、内存图表、连接跟踪与路由诊断）的功能说明与排错用法。

