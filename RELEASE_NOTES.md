## Ownbox v2.0.0 更新说明 / Release Notes

欢迎使用 Ownbox 2.0.0 专属增强版！本版本针对节点测速、单节点测试、界面细节与网络功能进行了全面重构与优化。

---

### 🚀 新增功能与体验优化

1. **新增“单节点测速”功能**：
   - 节点列表每一项右侧的分享按钮已替换为更符合规范的 **“三点菜单”**。
   - 点击三点菜单，首项即为 **“测当前节点速度”**，无需对整个订阅测速，即可秒级测出该节点的实际下载带宽与往返延迟。
   - 测速结果实时刷新在该节点的卡片上，并伴随顶部状态反馈。

2. **测速引擎彻底重构（解决点击无反应问题）**：
   - 修复了原测速机制无法连接本地代理导致测速假死无反应的底层架构问题。
   - 新测速引擎会为被测节点动态分配隔离测试通道，通过真实数据流实时采样计算下载速度（显示为 `↓ X.X MB/s (XXms)`）。
   - 内置防跑流量机制：单次测速最多采样 15MB 数据流或达到设定的超时时间后自动停止，兼顾精确度与节省流量。

3. **全协议栈扩充与 sing-box 1.13.16 架构升级**：
   - **新增协议支持**：完整支持 **Juicity**、**Snell**、**ShadowsocksR (SSR)** 以及 **VLESS XHTTP (SplitHTTP)** 传输驱动。
   - **内核升级**：全面升级至 **sing-box v1.13.16** 架构，支持 WireGuard endpoint 规范与共享链路防崩溃保护。

4. **WebDAV 远端同步与桌面备份导入**：
   - **WebDAV 同步**：支持将节点配置、订阅与设置自动/手动备份至坚果云、Nextcloud 或私有 WebDAV 服务器。
   - **桌面备份导入**：完美兼容导入桌面端导出的 `.thrbackup` 备份文件。

5. **个性化定制与界面升级**：
   - **自定义图标**：支持导入自定义图标包，个性化桌面快捷方式与快捷设置磁贴（Quick Settings Tile）。
   - **现代界面动效**：优化双列平板布局、Material 3 动态色彩与下拉组件。
   - **多语言切换**：支持应用内自由切换界面语言。

6. **双网络加速与纯 TUN 入站优化**：
   - **双网络加速**：通过 Android 底层网络连接守护机制，在连接 WiFi 时强制保持蜂窝移动数据处于唤醒状态，并发进行网络包调度。
   - **禁用混合入站**：开启后不在本地开放任何 2080 Socks/HTTP 端口，实现 100% 纯 TUN 虚拟网卡模式，杜绝端口探测与局域网泄露。

7. **界面优化与专属链接更新**：
   - 移除了关于界面中的“猫猫很可爱，请给猫猫捐款”按钮。
   - “源代码”链接直接指向 [OwnBoxForAndroid](https://github.com/Own716/OwnBoxForAndroid)。
   - “Telegram 更新频道”链接更新为用户的专属频道：[t.me/KarenOwn](https://t.me/KarenOwn)。
   - 软件内“检查更新”将自动检测并同步本仓库的最新 Releases。

8. **签名与版本规范**：
   - 修复安装包“缺少签名文件”的报错，所有构建出来的 APK 均附带完整的 V1 + V2 + V3 正式安全签名，手机可直接点击安装。
   - 安装包正式命名为 `Ownbox-2.0.0-arm64-v8a.apk` 等。
